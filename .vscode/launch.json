{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Locally",
      "request": "launch",
      "type": "node",
      "cwd": "${workspaceFolder}",
      "runtimeExecutable": "cds watch",
      "args": [
        "--profile",
        "hybrid"
      ],
      "skipFiles": [
        "<node_internals>/**"
      ]
    }
  ]
}